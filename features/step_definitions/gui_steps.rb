require 'rspec'
require 'selenium-webdriver'
driver = Selenium::WebDriver.for :firefox

When(/^user drags webpage down too mobile dimension$/) do
end

Then(/^the page should dynamically change to mobile layout$/) do
end
